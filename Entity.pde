class Entity {
  float x, y, w, h;
  // TOPIC: INHERITANCE - Base class for shared coordinate logic
  Entity(float x, float y, float w, float h) {
    this.x = x; this.y = y; this.w = w; this.h = h;
  }
}
