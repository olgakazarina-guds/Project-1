class Entity {
  float x, y, w, h;

  Entity(float x, float y, float w, float h) {
    // Pro-Tip: Use "this" to distinguish class fields from parameters
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
  }
}
